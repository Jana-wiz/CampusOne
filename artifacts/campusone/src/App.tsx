import { type ReactNode, useMemo, useState } from 'react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { ErrorBoundary } from '@/components/error-boundary';
import { Toaster } from '@/components/ui/toaster';
import { TooltipProvider } from '@/components/ui/tooltip';
import {
  AlertCircle, ArrowRight, BadgeCheck, Bell, BookOpen, Bookmark, BriefcaseBusiness, Building2, CalendarDays,
  Check, ChevronRight, CircleHelp, Clock3, Compass, ExternalLink, Eye, EyeOff, Filter, FolderOpen, GraduationCap,
  Heart, Home, KeyRound, Lightbulb, Link2, LoaderCircle, LockKeyhole, LogOut, Mail, MapPin, Menu, MessageCircle,
  MessageSquare, MoreHorizontal, Paperclip, PanelLeftClose, Phone, Pin, Plus, Search, Send, Settings2,
  Share2, ShieldCheck, Sparkles, Star, Trophy, UserPlus, UserRound, UsersRound, Video, X
} from 'lucide-react';
import { Link, Route, Switch, useLocation } from 'wouter';

const queryClient = new QueryClient();

type Student = { id: string; name: string; major: string; year: string; initials: string; color: string; bio?: string; tags?: string[]; connected?: boolean };
type Post = { id: string; author: Student; time: string; body: string; tag: string; likes: number; comments: number; saved?: boolean; liked?: boolean };

const currentStudent: Student = { id: 'me', name: 'Maya Rao', major: 'Human-Computer Interaction', year: '3rd year', initials: 'MR', color: 'from-[#e9a68e] to-[#bc5d49]', bio: 'Designing kinder technology and collecting campus stories.', tags: ['Product design', 'Film photography', 'Civic tech'] };
const students: Student[] = [
  { id: '1', name: 'Raghuvaran', major: 'Computer Science', year: '2nd year', initials: 'AM', color: 'from-[#7bb4ad] to-[#2f706d]', bio: 'Building tiny tools for big campus problems.', tags: ['React', 'Hackathons', 'Chess'] },
  { id: '2', name: 'Jersey', major: 'Psychology', year: '4th year', initials: 'NK', color: 'from-[#e5bd70] to-[#ca7947]', bio: 'Peer mentor, tea enthusiast, and listener.', tags: ['Mentorship', 'Research', 'Wellbeing'] },
  { id: '3', name: 'Agan', major: 'Mechanical Engineering', year: '3rd year', initials: 'DP', color: 'from-[#9d91bd] to-[#594d82]', bio: 'Making sustainable hardware less intimidating.', tags: ['CAD', 'Climate', 'Robotics'] },
  { id: '4', name: 'Shalini', major: 'Media Studies', year: '1st year', initials: 'SI', color: 'from-[#e59596] to-[#a94558]', bio: 'Finding my people one low-pressure hello at a time.', tags: ['Documentary', 'Music', 'Writing'] },
  { id: '5', name: 'Pari Vendhan', major: 'Data Science', year: '4th year', initials: 'KS', color: 'from-[#81a6c6] to-[#426081]', bio: 'Data by day, community gardener by weekend.', tags: ['Python', 'Open source', 'Gardening'] },
];
const initialPosts: Post[] = [
  { id: 'p1', author: students[1], time: '18 min ago', body: 'Starting a quiet study table in the north library this Thursday. No introductions required — just bring what you are working on and settle in. I will bring extra chargers and peppermint tea.', tag: 'Low-pressure connection', likes: 34, comments: 8 },
  { id: 'p2', author: students[0], time: '1 hr ago', body: 'Our 24-hour campus build night has two spots left for a designer and someone who loves talking to people. The idea is deliberately scrappy: make campus life a little easier for one person.', tag: 'Find teammates', likes: 61, comments: 14, saved: true },
  { id: 'p3', author: { ...students[3], name: 'The Common Room' }, time: '3 hrs ago', body: 'This week in the Common Room: zine-making, a first-gen student supper, and a prompt wall for the things we wish we knew in first year. Everyone belongs here.', tag: 'Campus life', likes: 48, comments: 11 },
];

const navItems = [
  { href: '/', label: 'Home', icon: Home },
  { href: '/connections', label: 'Connections', icon: UsersRound },
  { href: '/messages', label: 'Messages', icon: MessageCircle },
  { href: '/mentors', label: 'Mentors', icon: GraduationCap },
  { href: '/clubs', label: 'Clubs', icon: Building2 },
  { href: '/events', label: 'Events', icon: CalendarDays },
  { href: '/resources', label: 'Resources', icon: FolderOpen },
  { href: '/opportunities', label: 'Opportunities', icon: Compass },
];

function Avatar({ person, size = 'md', online = false }: { person: Student; size?: 'sm' | 'md' | 'lg' | 'xl'; online?: boolean }) {
  const dimensions = { sm: 'h-8 w-8 text-[11px]', md: 'h-10 w-10 text-xs', lg: 'h-14 w-14 text-base', xl: 'h-24 w-24 text-2xl' };
  return <span className={`relative inline-flex shrink-0 items-center justify-center rounded-[35%] bg-gradient-to-br ${person.color} font-bold text-white shadow-sm ${dimensions[size]}`} data-testid={`avatar-${person.id}`}>
    {person.initials}
    {online && <span className="absolute -bottom-0.5 -right-0.5 h-3 w-3 rounded-full border-2 border-[hsl(var(--card))] bg-[#63a88b]" />}
  </span>;
}

function Logo({ compact = false }: { compact?: boolean }) {
  return <Link href="/" className={`group flex items-center gap-2.5 ${compact ? 'justify-center' : ''}`} data-testid="link-logo">
    <span className="grid h-10 w-10 place-items-center rounded-xl bg-[hsl(var(--accent))] text-[hsl(var(--foreground))] shadow-sm transition-transform group-hover:rotate-3"><Sparkles size={19} strokeWidth={2.4} /></span>
    {!compact && <span className="font-serif text-[22px] font-bold tracking-tight text-[hsl(var(--sidebar-foreground))]">Campus<span className="text-[hsl(var(--sidebar-primary))]">One</span></span>}
  </Link>;
}

function Button({ children, variant = 'primary', className = '', onClick, type = 'button', disabled = false, testId }: { children: ReactNode; variant?: 'primary' | 'soft' | 'outline' | 'ghost' | 'dark'; className?: string; onClick?: () => void; type?: 'button' | 'submit'; disabled?: boolean; testId?: string }) {
  const styles = {
    primary: 'bg-[hsl(var(--primary))] text-white hover:brightness-95',
    soft: 'bg-[hsl(var(--accent))] text-[hsl(var(--foreground))] hover:brightness-97',
    outline: 'border border-[hsl(var(--border))] bg-transparent text-[hsl(var(--foreground))] hover:bg-[hsl(var(--muted))]',
    ghost: 'text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))] hover:text-[hsl(var(--foreground))]',
    dark: 'bg-[hsl(var(--sidebar))] text-white hover:brightness-110',
  };
  return <button type={type} disabled={disabled} onClick={onClick} data-testid={testId} className={`inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold transition-all duration-200 disabled:cursor-not-allowed disabled:opacity-55 ${styles[variant]} ${className}`}>{children}</button>;
}

function PageHeader({ eyebrow, title, description, action }: { eyebrow?: string; title: string; description?: string; action?: ReactNode }) {
  return <header className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end enter-up">
    <div>
      {eyebrow && <p className="mb-2 font-mono text-[10px] font-bold uppercase tracking-[.18em] text-[hsl(var(--primary))]">{eyebrow}</p>}
      <h1 className="font-serif text-4xl font-bold leading-[.95] tracking-[-.035em] text-[hsl(var(--foreground))] sm:text-5xl" data-testid="text-page-title">{title}</h1>
      {description && <p className="mt-3 max-w-xl text-sm leading-6 text-[hsl(var(--muted-foreground))]">{description}</p>}
    </div>
    {action}
  </header>;
}

function AppShell({ children }: { children: ReactNode }) {
  const [location, setLocation] = useLocation();
  const [mobileOpen, setMobileOpen] = useState(false);
  const isActive = (href: string) => href === '/' ? location === '/' : location.startsWith(href);
  return <div className="noise shell-bg min-h-[100dvh]">
    <aside className="fixed inset-y-0 left-0 z-40 hidden w-[244px] flex-col bg-[hsl(var(--sidebar))] px-4 py-5 text-[hsl(var(--sidebar-foreground))] md:flex">
      <div className="mb-9 px-3"><Logo /></div>
      <div className="mb-4 px-3 font-mono text-[9px] font-bold uppercase tracking-[.18em] text-[hsl(var(--sidebar-foreground)/.48)]">Your campus, together</div>
      <nav className="space-y-1">
        {navItems.map(({ href, label, icon: Icon }) => <Link key={href} href={href} data-testid={`link-nav-${label.toLowerCase()}`} className={`group flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm transition-colors ${isActive(href) ? 'bg-[hsl(var(--sidebar-accent))] font-semibold text-[hsl(var(--sidebar-primary))]' : 'text-[hsl(var(--sidebar-foreground)/.68)] hover:bg-[hsl(var(--sidebar-accent))] hover:text-[hsl(var(--sidebar-foreground))]'}`}><Icon size={18} strokeWidth={isActive(href) ? 2.4 : 1.8} /><span>{label}</span>{label === 'Messages' && <span className="ml-auto rounded-full bg-[hsl(var(--primary))] px-1.5 py-0.5 text-[10px] text-white">3</span>}</Link>)}
      </nav>
      <div className="my-6 border-t border-[hsl(var(--sidebar-border))]" />
      <div className="mb-3 px-3 font-mono text-[9px] font-bold uppercase tracking-[.18em] text-[hsl(var(--sidebar-foreground)/.48)]">Personal</div>
      <nav className="space-y-1">
        <Link href="/profile" data-testid="link-nav-profile" className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm transition-colors ${isActive('/profile') ? 'bg-[hsl(var(--sidebar-accent))] font-semibold text-[hsl(var(--sidebar-primary))]' : 'text-[hsl(var(--sidebar-foreground)/.68)] hover:bg-[hsl(var(--sidebar-accent))] hover:text-white'}`}><UserRound size={18} /><span>My profile</span></Link>
        <Link href="/notifications" data-testid="link-nav-notifications" className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm transition-colors ${isActive('/notifications') ? 'bg-[hsl(var(--sidebar-accent))] font-semibold text-[hsl(var(--sidebar-primary))]' : 'text-[hsl(var(--sidebar-foreground)/.68)] hover:bg-[hsl(var(--sidebar-accent))] hover:text-white'}`}><Bell size={18} /><span>Notifications</span><span className="ml-auto grid h-5 min-w-5 place-items-center rounded-full bg-[hsl(var(--accent))] px-1 text-[10px] font-bold text-[hsl(var(--foreground))]">4</span></Link>
        <Link href="/settings" data-testid="link-nav-settings" className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm transition-colors ${isActive('/settings') ? 'bg-[hsl(var(--sidebar-accent))] font-semibold text-[hsl(var(--sidebar-primary))]' : 'text-[hsl(var(--sidebar-foreground)/.68)] hover:bg-[hsl(var(--sidebar-accent))] hover:text-white'}`}><Settings2 size={18} /><span>Settings</span></Link>
      </nav>
      <div className="mt-auto">
        <div className="mb-3 rounded-2xl border border-[hsl(var(--sidebar-border))] bg-[hsl(var(--sidebar-accent)/.55)] p-3">
          <div className="mb-2 flex items-center gap-2"><ShieldCheck size={15} className="text-[hsl(var(--sidebar-primary))]" /><span className="text-[11px] font-semibold">Verified campus space</span></div>
          <p className="text-[10px] leading-4 text-[hsl(var(--sidebar-foreground)/.55)]">Only students with a campus email can join your community.</p>
        </div>
        <div className="flex items-center gap-3 rounded-xl p-2">
          <Avatar person={currentStudent} size="sm" online /><div className="min-w-0 flex-1"><p className="truncate text-xs font-semibold">{currentStudent.name}</p><p className="truncate text-[10px] text-[hsl(var(--sidebar-foreground)/.5)]">Northbridge University</p></div><button aria-label="Log out" data-testid="button-logout" onClick={() => setLocation('/login')} className="text-[hsl(var(--sidebar-foreground)/.48)] hover:text-white"><LogOut size={15} /></button>
        </div>
      </div>
    </aside>
    <header className="sticky top-0 z-30 flex h-[68px] items-center justify-between border-b border-[hsl(var(--border)/.75)] bg-[hsl(var(--background)/.9)] px-4 backdrop-blur-md md:hidden">
      <button onClick={() => setMobileOpen(!mobileOpen)} aria-label="Open navigation" data-testid="button-mobile-menu" className="rounded-lg p-2 hover:bg-[hsl(var(--muted))]">{mobileOpen ? <X size={21} /> : <Menu size={21} />}</button><Logo compact /><Link href="/notifications" data-testid="link-mobile-notifications" className="relative rounded-lg p-2"><Bell size={20} /><span className="absolute right-1 top-1 h-2 w-2 rounded-full bg-[hsl(var(--primary))]" /></Link>
    </header>
    {mobileOpen && <div className="fixed inset-0 z-20 bg-[hsl(var(--foreground)/.35)] md:hidden" onClick={() => setMobileOpen(false)}><div className="absolute left-0 top-0 h-full w-[265px] bg-[hsl(var(--sidebar))] px-5 py-5 text-white" onClick={(e) => e.stopPropagation()}><div className="mb-9"><Logo /></div>{navItems.concat([{ href: '/profile', label: 'My profile', icon: UserRound }]).map(({ href, label, icon: Icon }) => <Link onClick={() => setMobileOpen(false)} key={href} href={href} data-testid={`link-mobile-${label.toLowerCase()}`} className="flex items-center gap-3 rounded-xl px-3 py-3 text-sm text-white/75 hover:bg-white/10"><Icon size={18} />{label}</Link>)}<div className="my-4 border-t border-white/15" /><Link onClick={() => setMobileOpen(false)} href="/settings" data-testid="link-mobile-settings" className="flex items-center gap-3 rounded-xl px-3 py-3 text-sm text-white/75"><Settings2 size={18} />Settings</Link></div></div>}
    <main className="min-h-[100dvh] pb-20 md:ml-[244px] md:pb-0">{children}</main>
    <nav className="fixed bottom-0 left-0 right-0 z-30 flex h-[70px] items-center justify-around border-t border-[hsl(var(--border))] bg-[hsl(var(--card)/.96)] px-2 backdrop-blur-md md:hidden">
      {[navItems[0], navItems[1], navItems[2], navItems[5], { href: '/profile', label: 'Profile', icon: UserRound }].map(({ href, label, icon: Icon }) => <Link key={href} href={href} data-testid={`link-bottom-${label.toLowerCase()}`} className={`flex min-w-[52px] flex-col items-center gap-1 rounded-xl py-2 text-[10px] font-semibold ${isActive(href) ? 'text-[hsl(var(--primary))]' : 'text-[hsl(var(--muted-foreground))]'}`}><Icon size={19} /><span>{label}</span></Link>)}
    </nav>
  </div>;
}

function AuthPage({ mode }: { mode: 'login' | 'signup' }) {
  const [, setLocation] = useLocation();
  const [step, setStep] = useState<'form' | 'otp'>('form');
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [reg, setReg] = useState('');
  const [otp, setOtp] = useState('');
  const [showPassword, setShowPassword] = useState(false);
  const [rememberMe, setRememberMe] = useState(false);
  const [emailMode, setEmailMode] = useState(false);
  const [isSubmitting, setIsSubmitting] = useState(false);
  const [error, setError] = useState('');
  const [notice, setNotice] = useState('');
  const [success, setSuccess] = useState(false);
  const isSignup = mode === 'signup';
  const submit = (e: React.FormEvent) => {
    e.preventDefault();
    setError('');
    setNotice('');

    if (step === 'otp') {
      if (otp.length !== 6) {
        setError('Enter the 6-digit code from your college email.');
        return;
      }
      setIsSubmitting(true);
      setTimeout(() => {
        setIsSubmitting(false);
        setSuccess(true);
        setTimeout(() => setLocation('/dashboard'), 450);
      }, 1000);
      return;
    }

    if (!isSignup && emailMode && !email.trim()) {
      setError('College email cannot be empty.');
      return;
    }
    if ((isSignup || !emailMode) && !reg.trim()) {
      setError('Register number cannot be empty.');
      return;
    }
    if ((isSignup || emailMode) && email && !email.includes('@')) {
      setError('Enter a valid college email address.');
      return;
    }
    if (!password.trim()) {
      setError('Password cannot be empty.');
      return;
    }
    if (password.length < 8) {
      setError('Password must contain at least 8 characters.');
      return;
    }

    setIsSubmitting(true);
    setTimeout(() => {
      setIsSubmitting(false);
      if (isSignup) {
        setStep('otp');
        return;
      }
      setSuccess(true);
      setTimeout(() => setLocation('/dashboard'), 450);
    }, 1000);
  };

  const forgotPassword = () => {
    setError('');
    setNotice('If an account exists for this register number, reset instructions are on their way.');
  };

  return <div className="noise flex min-h-[100dvh] bg-[hsl(var(--background))]">
    <section className="relative hidden w-[43%] overflow-hidden bg-[hsl(var(--sidebar))] p-12 text-white lg:flex lg:flex-col">
      <div className="absolute -right-28 -top-24 h-80 w-80 rounded-full border-[38px] border-[hsl(var(--accent)/.18)]" /><div className="absolute -bottom-32 -left-20 h-96 w-96 rounded-full border-[55px] border-[hsl(var(--primary)/.14)]" />
      <Logo />
      <div className="relative mt-auto max-w-md pb-10"><p className="mb-5 font-mono text-[10px] uppercase tracking-[.2em] text-[hsl(var(--sidebar-primary))]">A softer way into campus life</p><h1 className="font-serif text-6xl font-bold leading-[.93] tracking-[-.04em]">Find your<br /><span className="text-[hsl(var(--sidebar-primary))]">people.</span><br />At your pace.</h1><p className="mt-6 max-w-sm text-sm leading-6 text-white/60">CampusOne is the trusted campus home base for the conversations, people, and opportunities that make university feel like yours.</p><div className="mt-10 flex items-center gap-5 border-t border-white/15 pt-5 text-xs text-white/60"><span className="flex items-center gap-2"><ShieldCheck size={16} className="text-[hsl(var(--sidebar-primary))]" />Verified students only</span><span className="flex items-center gap-2"><LockKeyhole size={15} className="text-[hsl(var(--sidebar-primary))]" />Private by design</span></div></div>
    </section>
    <section className="flex flex-1 items-center justify-center px-5 py-10 sm:px-10"><div className="w-full max-w-[450px]">
      <div className="mb-10 flex items-center justify-between lg:hidden"><Logo compact /><span className="font-serif text-xl font-bold">Campus<span className="text-[hsl(var(--primary))]">One</span></span></div>
       {success && <div role="status" aria-live="polite" className="fixed bottom-6 left-1/2 z-50 flex -translate-x-1/2 items-center gap-2 rounded-xl bg-[hsl(var(--sidebar))] px-4 py-3 text-sm font-semibold text-white shadow-xl"><Check size={17} className="text-[hsl(var(--sidebar-primary))]" />Welcome back to CampusOne!</div>}
       {step === 'form' ? <><div className="mb-8"><p className="mb-2 font-mono text-[10px] uppercase tracking-[.2em] text-[hsl(var(--primary))]">{isSignup ? 'Start with your campus identity' : 'CampusOne · Northbridge University'}</p><h2 className="font-serif text-4xl font-bold tracking-[-.035em]">{isSignup ? 'Create your account.' : <>Welcome Back <span aria-hidden="true">👋</span></>}</h2><p className="mt-3 text-sm text-[hsl(var(--muted-foreground))]">{isSignup ? 'Your official college details keep this space warm, real, and yours.' : 'Sign in to continue your campus journey.'}</p></div>
         <form onSubmit={submit} noValidate className="space-y-4">
           {(isSignup || !emailMode) && <label className="block"><span className="mb-1.5 block text-xs font-semibold">College Register Number</span><div className="relative"><KeyRound className="absolute left-3 top-3.5 text-[hsl(var(--muted-foreground))]" size={17} /><input autoComplete="username" value={reg} onChange={e => { setReg(e.target.value); setError(''); }} placeholder="e.g. NB-2024-01842" data-testid="input-registration-number" aria-invalid={!!error && !reg} className="h-12 w-full rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] pl-10 pr-3 text-sm outline-none transition focus:border-[hsl(var(--primary))]" /></div></label>}
           {(isSignup || emailMode) && <label className="block"><span className="mb-1.5 block text-xs font-semibold">College Email</span><div className="relative"><Mail className="absolute left-3 top-3.5 text-[hsl(var(--muted-foreground))]" size={17} /><input autoComplete="email" type="email" value={email} onChange={e => { setEmail(e.target.value); setError(''); }} placeholder="you@northbridge.edu" data-testid="input-college-email" className="h-12 w-full rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] pl-10 pr-3 text-sm outline-none transition focus:border-[hsl(var(--primary))]" /></div></label>}
           <label className="block"><div className="mb-1.5 flex justify-between text-xs font-semibold"><span>Password</span>{!isSignup && <button type="button" onClick={forgotPassword} data-testid="button-forgot-password" className="font-medium text-[hsl(var(--primary))] hover:underline">Forgot Password?</button>}</div><div className="relative"><LockKeyhole className="absolute left-3 top-3.5 text-[hsl(var(--muted-foreground))]" size={17} /><input autoComplete={isSignup ? 'new-password' : 'current-password'} required type={showPassword ? 'text' : 'password'} value={password} onChange={e => { setPassword(e.target.value); setError(''); }} placeholder="At least 8 characters" data-testid="input-password" className="h-12 w-full rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] pl-10 pr-12 text-sm outline-none transition focus:border-[hsl(var(--primary))]" /><button type="button" onClick={() => setShowPassword(!showPassword)} aria-label={showPassword ? 'Hide password' : 'Show password'} data-testid="button-toggle-password" className="absolute right-3 top-3 rounded-md p-1 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]">{showPassword ? <EyeOff size={17} /> : <Eye size={17} />}</button></div></label>
           {!isSignup && <label className="flex items-center gap-2 text-xs text-[hsl(var(--muted-foreground))]"><input type="checkbox" checked={rememberMe} onChange={e => setRememberMe(e.target.checked)} data-testid="checkbox-remember-me" className="h-4 w-4 accent-[hsl(var(--primary))]" />Remember Me</label>}
           {error && <p role="alert" className="flex items-center gap-2 rounded-xl border border-[hsl(var(--destructive)/.25)] bg-[hsl(var(--destructive)/.07)] px-3 py-2.5 text-xs font-semibold text-[hsl(var(--destructive))]"><AlertCircle size={15} />{error}</p>}
           {notice && <p role="status" className="rounded-xl border border-[hsl(var(--secondary)/.25)] bg-[hsl(var(--secondary)/.08)] px-3 py-2.5 text-xs font-semibold text-[hsl(var(--secondary))]">{notice}</p>}
           <Button type="submit" disabled={isSubmitting} className="mt-2 h-12 w-full" testId="button-submit-auth">{isSubmitting ? <><LoaderCircle size={17} className="animate-spin" />Signing in...</> : <>{isSignup ? 'Create Account' : 'Sign In'}<ArrowRight size={17} /></>}</Button>
         </form>
         {!isSignup && !emailMode && <><div className="my-6 flex items-center gap-3 text-[10px] font-bold uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]"><span className="h-px flex-1 bg-[hsl(var(--border))]" />OR<span className="h-px flex-1 bg-[hsl(var(--border))]" /></div><button type="button" onClick={() => { setEmailMode(true); setError(''); }} data-testid="button-college-email-login" className="flex h-12 w-full items-center justify-center gap-2 rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] text-sm font-semibold transition hover:border-[hsl(var(--primary))] hover:bg-[hsl(var(--muted)/.5)]"><Mail size={17} className="text-[hsl(var(--primary))]" />Continue with College Email</button></>}
         <p className="mt-7 text-center text-sm text-[hsl(var(--muted-foreground))]">{isSignup ? 'Already have an account?' : 'New to CampusOne?'} <Link href={isSignup ? '/login' : '/signup'} data-testid="link-switch-auth" className="font-bold text-[hsl(var(--primary))] hover:underline">{isSignup ? 'Sign in' : 'Create Account'}</Link></p>
       </> : <div className="enter-up"><button onClick={() => { setStep('form'); setError(''); }} data-testid="button-back-auth" className="mb-8 flex items-center gap-2 text-sm font-semibold text-[hsl(var(--muted-foreground))]"><ArrowRight size={16} className="rotate-180" />Back</button><div className="mb-8"><span className="mb-5 grid h-14 w-14 place-items-center rounded-2xl bg-[hsl(var(--accent))]"><Mail size={24} /></span><h2 className="font-serif text-4xl font-bold tracking-[-.035em]">Check your inbox.</h2><p className="mt-3 text-sm leading-6 text-[hsl(var(--muted-foreground))]">We sent a 6-digit code to <strong className="text-[hsl(var(--foreground))]">{email || 'your college email'}</strong>. It expires in 10 minutes.</p></div><form onSubmit={submit} noValidate className="space-y-4"><input autoFocus required minLength={6} maxLength={6} value={otp} onChange={e => { setOtp(e.target.value.replace(/\D/g, '')); setError(''); }} inputMode="numeric" placeholder="000000" aria-label="6-digit email verification code" data-testid="input-otp" className="h-14 w-full rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] text-center font-mono text-2xl tracking-[.4em] outline-none focus:border-[hsl(var(--primary))]" />{error && <p role="alert" className="flex items-center gap-2 rounded-xl border border-[hsl(var(--destructive)/.25)] bg-[hsl(var(--destructive)/.07)] px-3 py-2.5 text-xs font-semibold text-[hsl(var(--destructive))]"><AlertCircle size={15} />{error}</p>}<Button type="submit" disabled={isSubmitting} className="h-12 w-full" testId="button-verify-otp">{isSubmitting ? <><LoaderCircle size={17} className="animate-spin" />Verifying...</> : <>Verify and enter <ArrowRight size={17} /></>}</Button></form><button type="button" onClick={() => setNotice('A fresh verification code has been sent.')} data-testid="button-resend-otp" className="mt-5 w-full text-center text-sm font-semibold text-[hsl(var(--primary))]">Resend code</button>{notice && <p role="status" className="mt-3 text-center text-xs font-semibold text-[hsl(var(--secondary))]">{notice}</p>}</div>}
      <p className="mt-12 flex items-center justify-center gap-2 text-center text-xs text-[hsl(var(--muted-foreground))]"><ShieldCheck size={15} />CampusOne is exclusive to verified Northbridge students.</p>
    </div></section>
  </div>;
}

function HomePage() {
  const [posts, setPosts] = useState(initialPosts);
  const [composer, setComposer] = useState('');
  const [posted, setPosted] = useState(false);
  const togglePost = (id: string, key: 'liked' | 'saved') => setPosts(p => p.map(x => x.id === id ? { ...x, [key]: !x[key], likes: key === 'liked' ? x.likes + (x.liked ? -1 : 1) : x.likes } : x));
  const addPost = () => { if (!composer.trim()) return; setPosts([{ id: `new-${Date.now()}`, author: currentStudent, time: 'just now', body: composer, tag: 'From your campus', likes: 0, comments: 0 }, ...posts]); setComposer(''); setPosted(true); setTimeout(() => setPosted(false), 2200); };
  return <div className="mx-auto max-w-[1240px] px-4 py-7 sm:px-7 lg:px-10">
    <div className="mb-9 flex items-start justify-between enter-up"><div><p className="mb-2 font-mono text-[10px] font-bold uppercase tracking-[.18em] text-[hsl(var(--primary))]">Thursday, September 17 · Chennai</p><h1 className="font-serif text-4xl font-bold tracking-[-.04em] sm:text-5xl">Good morning, Maya<span className="text-[hsl(var(--primary))]">.</span></h1><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">Here’s what’s happening in your corner of campus.</p></div><div className="hidden items-center gap-3 sm:flex"><Link href="/notifications" data-testid="link-home-notifications" className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3 text-[hsl(var(--muted-foreground))] transition hover:text-[hsl(var(--primary))]"><Bell size={19} /></Link><Link href="/profile" data-testid="link-home-profile"><Avatar person={currentStudent} size="md" online /></Link></div></div>
    <div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_310px]">
      <section className="min-w-0">
        <div className="campus-card campus-card-dark mb-6 overflow-hidden border-0 text-white enter-up enter-up-delay-1"><div className="relative px-5 py-6 sm:px-7 sm:py-7"><div className="absolute -right-8 -top-16 h-48 w-48 rounded-full border-[22px] border-[hsl(var(--accent)/.15)]" /><div className="relative max-w-xl"><div className="mb-4 flex items-center gap-2 text-[hsl(var(--sidebar-primary))]"><Sparkles size={16} /><span className="font-mono text-[10px] font-bold uppercase tracking-[.18em]">Your campus pulse</span></div><h2 className="font-serif text-3xl font-bold leading-tight">A small hello can change<br className="hidden sm:block" /> the shape of your week.</h2><p className="mt-3 max-w-md text-sm leading-6 text-white/60">Meet people through the things you already care about — quietly, deliberately, and without the awkwardness.</p><Link href="/connections" data-testid="link-hero-connections" className="mt-5 inline-flex items-center gap-2 rounded-xl bg-[hsl(var(--accent))] px-4 py-2.5 text-sm font-bold text-[hsl(var(--foreground))] transition hover:brightness-95">Find your people <ArrowRight size={16} /></Link></div></div><div className="flex gap-0 border-t border-white/10 bg-white/[.04]"><div className="flex-1 px-5 py-3 sm:px-7"><p className="font-mono text-lg font-bold text-[hsl(var(--sidebar-primary))]">1,284</p><p className="text-[10px] text-white/45">students connected</p></div><div className="flex-1 border-l border-white/10 px-5 py-3 sm:px-7"><p className="font-mono text-lg font-bold text-[hsl(var(--sidebar-primary))]">46</p><p className="text-[10px] text-white/45">things to join today</p></div><div className="hidden flex-1 border-l border-white/10 px-5 py-3 sm:block sm:px-7"><p className="font-mono text-lg font-bold text-[hsl(var(--sidebar-primary))]">100%</p><p className="text-[10px] text-white/45">campus verified</p></div></div></div>
        <div className="campus-card mb-6 p-4 enter-up enter-up-delay-2 sm:p-5"><div className="flex gap-3"><Avatar person={currentStudent} size="md" /><div className="flex-1"><textarea value={composer} onChange={e => setComposer(e.target.value)} placeholder="What’s on your campus mind?" data-testid="input-compose-post" rows={2} className="w-full resize-none bg-transparent pt-1 text-sm outline-none placeholder:text-[hsl(var(--muted-foreground))]" /><div className="mt-3 flex items-center justify-between border-t border-[hsl(var(--border)/.7)] pt-3"><div className="flex gap-1 text-[hsl(var(--muted-foreground))]"><button aria-label="Attach a resource" data-testid="button-attach-post" className="rounded-lg p-2 hover:bg-[hsl(var(--muted))]"><Paperclip size={17} /></button><button aria-label="Add a topic" data-testid="button-topic-post" className="rounded-lg p-2 hover:bg-[hsl(var(--muted))]"><Pin size={17} /></button></div><div className="flex items-center gap-3">{posted && <span className="text-xs font-semibold text-[hsl(var(--secondary))]">Posted to campus</span>}<Button onClick={addPost} className="px-4 py-2" testId="button-submit-post"><Plus size={16} />Share</Button></div></div></div></div></div>
        <div className="mb-4 flex items-center justify-between"><h2 className="font-serif text-2xl font-bold">From your campus</h2><button data-testid="button-feed-filter" className="flex items-center gap-2 rounded-lg px-2 py-1 text-xs font-semibold text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]"><Filter size={14} /> Curated for you</button></div>
        <div className="space-y-4">{posts.map((post, i) => <PostCard key={post.id} post={post} onToggle={togglePost} index={i} />)}</div>
      </section>
      <aside className="space-y-5">
        <div className="campus-card p-5 enter-up enter-up-delay-2"><div className="mb-4 flex items-center justify-between"><h3 className="font-serif text-xl font-bold">Your week</h3><Link href="/events" data-testid="link-week-events" className="text-xs font-bold text-[hsl(var(--primary))]">See all</Link></div><div className="space-y-3"><MiniEvent day="18" month="SEP" title="Designing for belonging" meta="4:30 PM · Studio 2" color="bg-[#e9a68e]" /><MiniEvent day="19" month="SEP" title="Quiet study table" meta="All day · North Library" color="bg-[#7bb4ad]" /><MiniEvent day="21" month="SEP" title="Campus build night" meta="7:00 PM · Makerspace" color="bg-[#e5bd70]" /></div></div>
        <div className="campus-card overflow-hidden p-5 enter-up enter-up-delay-3"><div className="mb-4 flex items-center gap-2"><div className="grid h-8 w-8 place-items-center rounded-lg bg-[hsl(var(--accent))]"><Sparkles size={15} /></div><h3 className="font-serif text-xl font-bold">For your orbit</h3></div><p className="mb-4 text-xs leading-5 text-[hsl(var(--muted-foreground))]">People who share two of your interests are looking for a connection.</p><div className="space-y-3">{students.slice(0, 3).map(person => <div key={person.id} className="flex items-center gap-3"><Avatar person={person} size="sm" online /><div className="min-w-0 flex-1"><p className="truncate text-xs font-bold">{person.name}</p><p className="truncate text-[10px] text-[hsl(var(--muted-foreground))]">{person.tags?.slice(0, 2).join(' · ')}</p></div><Link href="/connections" data-testid={`link-orbit-${person.id}`} className="rounded-lg border border-[hsl(var(--border))] p-1.5 text-[hsl(var(--primary))]"><UserPlus size={14} /></Link></div>)}</div><Link href="/connections" data-testid="link-see-connections" className="mt-4 flex items-center justify-between border-t border-[hsl(var(--border))] pt-3 text-xs font-bold text-[hsl(var(--primary))]">Explore connections <ArrowRight size={14} /></Link></div>
        <div className="px-2 text-[10px] leading-5 text-[hsl(var(--muted-foreground))]">CampusOne · A trusted space for Northbridge students<br />Built for real connection, at your pace.</div>
      </aside>
    </div>
  </div>;
}

function PostCard({ post, onToggle, index }: { post: Post; onToggle: (id: string, key: 'liked' | 'saved') => void; index: number }) {
  return <article className={`campus-card campus-card-lift p-5 enter-up enter-up-delay-${Math.min(index + 1, 3)}`} data-testid={`card-post-${post.id}`}><div className="flex items-start gap-3"><Avatar person={post.author} size="md" online /><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><div><p className="text-sm font-bold">{post.author.name} {post.author.id !== 'me' && <BadgeCheck size={14} className="inline text-[hsl(var(--secondary))]" />}</p><p className="mt-0.5 text-[11px] text-[hsl(var(--muted-foreground))]">{post.author.major} · {post.time}</p></div><button aria-label="Post options" data-testid={`button-post-options-${post.id}`} className="rounded-lg p-1 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]"><MoreHorizontal size={18} /></button></div><p className="mt-4 text-sm leading-6">{post.body}</p><span className="mt-4 inline-flex rounded-full bg-[hsl(var(--muted))] px-2.5 py-1 font-mono text-[9px] font-bold uppercase tracking-[.08em] text-[hsl(var(--muted-foreground))]">{post.tag}</span><div className="mt-5 flex items-center justify-between border-t border-[hsl(var(--border))] pt-3"><div className="flex gap-1"><button onClick={() => onToggle(post.id, 'liked')} aria-label="Like post" data-testid={`button-like-${post.id}`} className={`flex items-center gap-1.5 rounded-lg px-2 py-1.5 text-xs font-semibold transition ${post.liked ? 'text-[hsl(var(--primary))]' : 'text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]'}`}><Heart size={16} fill={post.liked ? 'currentColor' : 'none'} />{post.likes}</button><button aria-label="Comment on post" data-testid={`button-comment-${post.id}`} className="flex items-center gap-1.5 rounded-lg px-2 py-1.5 text-xs font-semibold text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]"><MessageSquare size={16} />{post.comments}</button><button aria-label="Share post" data-testid={`button-share-${post.id}`} className="rounded-lg p-1.5 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]"><Share2 size={16} /></button></div><button onClick={() => onToggle(post.id, 'saved')} aria-label="Save post" data-testid={`button-save-${post.id}`} className={`rounded-lg p-1.5 ${post.saved ? 'text-[hsl(var(--primary))]' : 'text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]'}`}><Bookmark size={16} fill={post.saved ? 'currentColor' : 'none'} /></button></div></div></div></article>;
}

function MiniEvent({ day, month, title, meta, color }: { day: string; month: string; title: string; meta: string; color: string }) {
  return <div className="flex items-center gap-3"><div className={`grid h-11 w-11 shrink-0 place-items-center rounded-xl ${color}`}><div className="text-center"><p className="font-mono text-sm font-bold leading-none">{day}</p><p className="mt-0.5 text-[8px] font-bold">{month}</p></div></div><div className="min-w-0"><p className="truncate text-xs font-bold">{title}</p><p className="mt-0.5 text-[10px] text-[hsl(var(--muted-foreground))]">{meta}</p></div></div>;
}

function ConnectionsPage() {
  const [query, setQuery] = useState('');
  const [filter, setFilter] = useState('Everyone');
  const [connected, setConnected] = useState<string[]>(['2']);
  const filtered = students.filter(s => s.name.toLowerCase().includes(query.toLowerCase()) || s.major.toLowerCase().includes(query.toLowerCase()) || s.tags?.some(t => t.toLowerCase().includes(query.toLowerCase()))).filter(s => filter === 'Everyone' || s.year === filter);
  return <div className="mx-auto max-w-[1100px] px-4 py-7 sm:px-7 lg:px-10"><PageHeader eyebrow="The people layer" title="Find your people." description="Search by a shared interest, a familiar major, or simply the kind of energy you want around you." action={<Button variant="soft" testId="button-invite-connection"><Plus size={17} />Invite someone</Button>} />
    <div className="mb-7 flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3.5 top-3.5 text-[hsl(var(--muted-foreground))]" size={18} /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Try “film”, “data”, or a name" data-testid="input-search-connections" className="h-12 w-full rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] pl-11 pr-4 text-sm shadow-sm outline-none focus:border-[hsl(var(--primary))]" /></div><select value={filter} onChange={e => setFilter(e.target.value)} data-testid="select-connection-filter" className="h-12 rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-4 text-sm font-semibold outline-none"><option>Everyone</option><option>1st year</option><option>2nd year</option><option>3rd year</option><option>4th year</option></select></div>
    <div className="mb-5 flex items-center justify-between"><p className="text-xs text-[hsl(var(--muted-foreground))]"><strong className="text-[hsl(var(--foreground))]">{filtered.length}</strong> students in your orbit</p><button data-testid="button-connection-filter" className="flex items-center gap-2 text-xs font-semibold text-[hsl(var(--muted-foreground))]"><Filter size={14} /> More filters</button></div>
    {filtered.length ? <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(person => <div key={person.id} className="campus-card campus-card-lift p-5" data-testid={`card-student-${person.id}`}><div className="flex items-start justify-between"><Avatar person={person} size="lg" online={person.id === '1' || person.id === '4'} /><span className="rounded-full bg-[hsl(var(--muted))] px-2 py-1 text-[9px] font-bold uppercase tracking-wider text-[hsl(var(--muted-foreground))]">{person.year}</span></div><h3 className="mt-4 font-serif text-xl font-bold">{person.name} <BadgeCheck size={15} className="inline text-[hsl(var(--secondary))]" /></h3><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{person.major}</p><p className="mt-3 min-h-10 text-xs leading-5 text-[hsl(var(--muted-foreground))]">{person.bio}</p><div className="mt-4 flex flex-wrap gap-1.5">{person.tags?.map(tag => <span key={tag} className="rounded-md bg-[hsl(var(--muted))] px-2 py-1 text-[10px]">{tag}</span>)}</div><div className="mt-5 flex gap-2"><Button variant={connected.includes(person.id) ? 'soft' : 'primary'} className="flex-1 px-2 py-2" onClick={() => setConnected(c => c.includes(person.id) ? c.filter(i => i !== person.id) : [...c, person.id])} testId={`button-connect-${person.id}`}>{connected.includes(person.id) ? <><Check size={15} />Connected</> : <><UserPlus size={15} />Connect</>}</Button><Link href="/messages" data-testid={`link-message-${person.id}`} className="grid w-10 place-items-center rounded-xl border border-[hsl(var(--border))] text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]"><MessageCircle size={17} /></Link></div></div>)}</div> : <EmptyState icon={Search} title="No one by that description yet" copy="Try a broader interest or search across a different year." action={<Button onClick={() => { setQuery(''); setFilter('Everyone'); }} testId="button-clear-connection-search">Clear search</Button>} />}</div>;
}

function MessagesPage() {
  const [active, setActive] = useState('a');
  const [text, setText] = useState('');
  const [sent, setSent] = useState<Record<string, string[]>>({ a: ['You mentioned a quiet study table — I would love to join.', 'Perfect. I’ll save you a seat near the windows.'] });
  const conversations = [{ id: 'a', person: students[1], preview: 'Perfect. I’ll save you a seat near the windows.', time: '9:42 AM', unread: 2 }, { id: 'b', person: students[0], preview: 'The build night idea is getting exciting.', time: 'Yesterday', unread: 1 }, { id: 'c', person: students[3], preview: 'Thank you for sharing that resource!', time: 'Mon', unread: 0 }];
  const current = conversations.find(c => c.id === active) ?? conversations[0];
  const send = () => { if (!text.trim()) return; setSent(s => ({ ...s, [active]: [...(s[active] || []), text] })); setText(''); };
  return <div className="mx-auto max-w-[1200px] px-4 py-7 sm:px-7 lg:px-10"><PageHeader eyebrow="Your conversations" title="Messages." description="Small conversations, real campus context." action={<Button variant="soft" testId="button-new-message"><Plus size={17} />New message</Button>} /><div className="campus-card grid min-h-[580px] overflow-hidden md:grid-cols-[310px_1fr]"><aside className="border-b border-[hsl(var(--border))] md:border-b-0 md:border-r"><div className="border-b border-[hsl(var(--border))] p-4"><div className="relative"><Search className="absolute left-3 top-2.5 text-[hsl(var(--muted-foreground))]" size={16} /><input placeholder="Search messages" data-testid="input-search-messages" className="h-9 w-full rounded-lg bg-[hsl(var(--muted))] pl-9 text-xs outline-none" /></div></div><div className="scrollbar-thin flex max-h-[180px] overflow-y-auto md:max-h-[490px] md:flex-col">{conversations.map(c => <button key={c.id} onClick={() => setActive(c.id)} data-testid={`button-conversation-${c.id}`} className={`flex min-w-[230px] items-center gap-3 border-b border-[hsl(var(--border)/.7)] p-4 text-left transition ${active === c.id ? 'bg-[hsl(var(--muted))]' : 'hover:bg-[hsl(var(--muted)/.55)]'}`}><Avatar person={c.person} size="sm" online={c.id === 'a'} /><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><p className="truncate text-xs font-bold">{c.person.name}</p><span className="text-[9px] text-[hsl(var(--muted-foreground))]">{c.time}</span></div><p className="mt-1 truncate text-[10px] text-[hsl(var(--muted-foreground))]">{c.preview}</p></div>{c.unread > 0 && <span className="grid h-5 min-w-5 place-items-center rounded-full bg-[hsl(var(--primary))] px-1 text-[10px] font-bold text-white">{c.unread}</span>}</button>)}</div></aside><section className="flex min-h-[440px] flex-col"><div className="flex items-center gap-3 border-b border-[hsl(var(--border))] p-4"><Avatar person={current.person} size="sm" online /><div className="flex-1"><p className="text-sm font-bold">{current.person.name}</p><p className="text-[10px] text-[hsl(var(--secondary))]">Active now · {current.person.major}</p></div><button aria-label="Start video call" data-testid="button-video-call" className="rounded-lg p-2 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]"><Video size={17} /></button><button aria-label="Call student" data-testid="button-phone-call" className="rounded-lg p-2 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]"><Phone size={17} /></button></div><div className="scrollbar-thin flex flex-1 flex-col justify-end gap-3 overflow-y-auto p-5">{(sent[active] || ['Hey! I saw your note on the campus feed.', 'Always happy to find a familiar face here.']).map((message, i) => <div key={`${message}-${i}`} className={`max-w-[78%] rounded-2xl px-4 py-3 text-sm leading-5 ${i % 2 === 0 ? 'self-start rounded-bl-md bg-[hsl(var(--muted))]' : 'self-end rounded-br-md bg-[hsl(var(--sidebar))] text-white'}`} data-testid={`message-bubble-${i}`}>{message}<p className={`mt-1 text-[9px] ${i % 2 === 0 ? 'text-[hsl(var(--muted-foreground))]' : 'text-white/50'}`}>{i % 2 === 0 ? 'Seen' : '9:42 AM'}</p></div>)}</div><div className="border-t border-[hsl(var(--border))] p-4"><div className="flex items-center gap-2 rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--muted)/.55)] px-3"><button aria-label="Attach file" data-testid="button-attach-message" className="text-[hsl(var(--muted-foreground))]"><Paperclip size={17} /></button><input value={text} onChange={e => setText(e.target.value)} onKeyDown={e => e.key === 'Enter' && send()} placeholder="Write a message..." data-testid="input-message" className="h-11 flex-1 bg-transparent text-sm outline-none" /><button onClick={send} aria-label="Send message" data-testid="button-send-message" className="grid h-8 w-8 place-items-center rounded-lg bg-[hsl(var(--primary))] text-white hover:brightness-95"><Send size={15} /></button></div></div></section></div></div>;
}

function MentorsPage() {
  const [query, setQuery] = useState('');
  const [requested, setRequested] = useState<string[]>([]);
  const mentors = students.filter((s) => s.id === '2' || s.id === '5').map((s, i) => ({ ...s, role: i ? 'Career & data' : 'First-year transition', sessions: i ? 18 : 24, quote: i ? 'The best project is one that teaches you something true.' : 'You do not need to have it all figured out in your first semester.' }));
  const visible = mentors.filter(m => `${m.name} ${m.major} ${m.role}`.toLowerCase().includes(query.toLowerCase()));
  return <div className="mx-auto max-w-[1100px] px-4 py-7 sm:px-7 lg:px-10"><PageHeader eyebrow="Guidance from a step ahead" title="Mentors who remember." description="Verified seniors who have been through the wobbly bits and are happy to share what helped." action={<Button variant="soft" testId="button-become-mentor"><GraduationCap size={17} />Become a mentor</Button>} /><div className="campus-card mb-7 flex flex-col gap-5 bg-[hsl(var(--sidebar))] p-5 text-white sm:flex-row sm:items-center sm:justify-between sm:p-7"><div><p className="mb-2 font-mono text-[10px] uppercase tracking-[.18em] text-[hsl(var(--sidebar-primary))]">Mentor matching</p><h2 className="font-serif text-3xl font-bold">A thoughtful question<br />is a great place to start.</h2></div><div className="max-w-xs text-sm leading-6 text-white/60">Choose someone by the kind of support you need. One message is all it takes to begin.</div></div><div className="mb-6 relative max-w-md"><Search className="absolute left-3.5 top-3.5 text-[hsl(var(--muted-foreground))]" size={18} /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search mentorship areas" data-testid="input-search-mentors" className="h-12 w-full rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] pl-11 text-sm outline-none focus:border-[hsl(var(--primary))]" /></div><div className="grid gap-4 md:grid-cols-2">{visible.map(mentor => <div key={mentor.id} className="campus-card campus-card-lift p-5 sm:p-6" data-testid={`card-mentor-${mentor.id}`}><div className="flex items-start gap-4"><Avatar person={mentor} size="lg" online /><div className="flex-1"><h3 className="font-serif text-2xl font-bold">{mentor.name} <BadgeCheck size={16} className="inline text-[hsl(var(--secondary))]" /></h3><p className="text-xs text-[hsl(var(--muted-foreground))]">{mentor.major} · {mentor.year}</p><span className="mt-3 inline-flex rounded-full bg-[hsl(var(--accent)/.55)] px-2.5 py-1 text-[10px] font-bold">{mentor.role}</span></div></div><p className="mt-5 border-l-2 border-[hsl(var(--primary))] pl-4 font-serif text-base italic leading-6 text-[hsl(var(--muted-foreground))]">“{mentor.quote}”</p><div className="mt-5 flex items-center justify-between border-t border-[hsl(var(--border))] pt-4"><span className="flex items-center gap-1.5 text-xs text-[hsl(var(--muted-foreground))]"><Star size={14} className="text-[hsl(var(--accent-foreground))]" fill="hsl(var(--accent-foreground))" /> {mentor.sessions} students supported</span><Button variant={requested.includes(mentor.id) ? 'soft' : 'primary'} className="px-3 py-2" onClick={() => setRequested(r => r.includes(mentor.id) ? r.filter(x => x !== mentor.id) : [...r, mentor.id])} testId={`button-request-mentor-${mentor.id}`}>{requested.includes(mentor.id) ? <><Check size={14} />Requested</> : <>Ask for guidance <ArrowRight size={14} /></>}</Button></div></div>)}</div></div>;
}

function ClubsPage() {
  const [joined, setJoined] = useState<string[]>(['c1']);
  const [query, setQuery] = useState('');
  const clubs = [{ id: 'c1', name: 'The Common Room', category: 'Community & culture', members: 142, description: 'A warm, cross-campus space for stories, zines, and the conversations between classes.', icon: BookOpen, color: 'bg-[#e9a68e]' }, { id: 'c2', name: 'Northbridge Product Lab', category: 'Build & experiment', members: 86, description: 'Students making useful, thoughtful products for the people around us.', icon: Lightbulb, color: 'bg-[#e5bd70]' }, { id: 'c3', name: 'Frame by Frame', category: 'Arts & media', members: 57, description: 'A low-pressure film collective for watching, making, and talking about images.', icon: Compass, color: 'bg-[#9d91bd]' }, { id: 'c4', name: 'Open Source Circle', category: 'Technology', members: 104, description: 'Pair, learn, and ship small contributions to projects that matter.', icon: Link2, color: 'bg-[#7bb4ad]' }];
  const visible = clubs.filter(c => `${c.name} ${c.category} ${c.description}`.toLowerCase().includes(query.toLowerCase()));
  return <div className="mx-auto max-w-[1100px] px-4 py-7 sm:px-7 lg:px-10"><PageHeader eyebrow="Find a room with your name on it" title="Campus clubs." description="Join the things that make a big campus feel like a collection of small, familiar rooms." action={<Button variant="soft" testId="button-start-club"><Plus size={17} />Start a club</Button>} /><div className="mb-7 relative max-w-md"><Search className="absolute left-3.5 top-3.5 text-[hsl(var(--muted-foreground))]" size={18} /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search clubs or interests" data-testid="input-search-clubs" className="h-12 w-full rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] pl-11 text-sm outline-none focus:border-[hsl(var(--primary))]" /></div><div className="grid gap-4 md:grid-cols-2">{visible.map(club => { const Icon = club.icon; return <div key={club.id} className="campus-card campus-card-lift overflow-hidden" data-testid={`card-club-${club.id}`}><div className={`flex h-24 items-end justify-between ${club.color} p-5`}><div className="grid h-12 w-12 place-items-center rounded-2xl bg-white/75"><Icon size={23} /></div><span className="rounded-full bg-white/70 px-2.5 py-1 font-mono text-[9px] font-bold uppercase tracking-wider">{club.category}</span></div><div className="p-5"><h3 className="font-serif text-2xl font-bold">{club.name}</h3><p className="mt-2 min-h-12 text-sm leading-5 text-[hsl(var(--muted-foreground))]">{club.description}</p><div className="mt-5 flex items-center justify-between border-t border-[hsl(var(--border))] pt-4"><span className="flex items-center gap-1.5 text-xs text-[hsl(var(--muted-foreground))]"><UsersRound size={14} /> {club.members} members</span><Button variant={joined.includes(club.id) ? 'soft' : 'primary'} className="px-3 py-2" onClick={() => setJoined(j => j.includes(club.id) ? j.filter(x => x !== club.id) : [...j, club.id])} testId={`button-join-club-${club.id}`}>{joined.includes(club.id) ? <><Check size={14} />Joined</> : <>Join club <ArrowRight size={14} /></>}</Button></div></div></div>; })}</div></div>;
}

function EventsPage() {
  const [interested, setInterested] = useState<string[]>(['e2']);
  const [registered, setRegistered] = useState<string[]>([]);
  const events = [{ id: 'e1', date: '18', month: 'SEP', title: 'Designing for belonging', category: 'Workshop', time: '4:30–6:00 PM', place: 'Studio 2 · Arts building', color: 'bg-[#e9a68e]', host: 'Northbridge Product Lab' }, { id: 'e2', date: '19', month: 'SEP', title: 'Quiet study table', category: 'Low pressure', time: 'All day', place: 'North Library · window seats', color: 'bg-[#7bb4ad]', host: 'The Common Room' }, { id: 'e3', date: '21', month: 'SEP', title: 'Campus build night', category: 'Hackathon', time: '7:00 PM–7:00 PM', place: 'Makerspace · west quad', color: 'bg-[#e5bd70]', host: 'Product Lab + Open Source Circle' }, { id: 'e4', date: '24', month: 'SEP', title: 'First-gen student supper', category: 'Community', time: '6:00–8:00 PM', place: 'The Hearth · student centre', color: 'bg-[#9d91bd]', host: 'First-Gen Network' }];
  return <div className="mx-auto max-w-[1050px] px-4 py-7 sm:px-7 lg:px-10"><PageHeader eyebrow="There is something for you this week" title="Campus events." description="A handpicked calendar of things worth leaving your room for — and a few that are not." action={<Button variant="soft" testId="button-create-event"><Plus size={17} />Create an event</Button>} /><div className="mb-7 flex gap-2 overflow-x-auto pb-1"><button data-testid="button-event-filter-all" className="rounded-full bg-[hsl(var(--sidebar))] px-4 py-2 text-xs font-bold text-white">All events</button>{['Today', 'This week', 'Workshops', 'Low pressure'].map(x => <button key={x} data-testid={`button-event-filter-${x.toLowerCase().replace(' ', '-')}`} className="whitespace-nowrap rounded-full border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-4 py-2 text-xs font-semibold text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]">{x}</button>)}</div><div className="space-y-4">{events.map(event => <div key={event.id} className="campus-card campus-card-lift flex flex-col overflow-hidden sm:flex-row" data-testid={`card-event-${event.id}`}><div className={`flex h-28 shrink-0 items-center justify-center ${event.color} sm:h-auto sm:w-28`}><div className="text-center"><p className="font-mono text-4xl font-bold leading-none">{event.date}</p><p className="mt-1 font-mono text-[10px] font-bold tracking-wider">{event.month}</p></div></div><div className="flex flex-1 flex-col justify-between gap-4 p-5 sm:flex-row sm:items-center"><div><span className="font-mono text-[9px] font-bold uppercase tracking-[.14em] text-[hsl(var(--primary))]">{event.category}</span><h3 className="mt-1 font-serif text-2xl font-bold">{event.title}</h3><p className="mt-2 text-xs text-[hsl(var(--muted-foreground))]">{event.time} <span className="mx-1.5">·</span> {event.place}</p><p className="mt-2 text-[11px] text-[hsl(var(--muted-foreground))]">Hosted by <strong className="text-[hsl(var(--foreground))]">{event.host}</strong></p></div><div className="flex shrink-0 items-center gap-2"><Button variant={interested.includes(event.id) ? 'soft' : 'outline'} className="px-3 py-2" onClick={() => setInterested(i => i.includes(event.id) ? i.filter(x => x !== event.id) : [...i, event.id])} testId={`button-interest-event-${event.id}`}>{interested.includes(event.id) ? <><Check size={14} />Interested</> : 'I’m interested'}</Button><Button variant={registered.includes(event.id) ? 'soft' : 'primary'} className="px-3 py-2" onClick={() => setRegistered(r => r.includes(event.id) ? r.filter(x => x !== event.id) : [...r, event.id])} testId={`button-register-event-${event.id}`}>{registered.includes(event.id) ? 'Registered' : 'Register'}<ArrowRight size={14} /></Button></div></div></div>)}</div></div>;
}

function ResourcesPage() {
  const [active, setActive] = useState('All resources');
  const folders = [{ name: 'First year, without the panic', count: '34 files', icon: Compass, color: 'bg-[#e9a68e]' }, { name: 'HCI studio notes', count: '18 files', icon: BookOpen, color: 'bg-[#7bb4ad]' }, { name: 'Placement season', count: '27 files', icon: BriefcaseBusiness, color: 'bg-[#e5bd70]' }];
  const materials = [{ title: 'The honest guide to choosing electives', by: 'Jersey', type: 'Guide', time: 'updated 2h ago' }, { title: 'Product design critique checklist', by: 'Raghuvaran', type: 'Template', time: 'updated yesterday' }, { title: 'Northern research opportunities · 2024', by: 'Career Centre', type: 'Collection', time: 'updated 3 days ago' }, { title: 'Calculus II: the week 7 rescue sheet', by: 'Maya Chen', type: 'Notes', time: 'updated 5 days ago' }];
  return <div className="mx-auto max-w-[1100px] px-4 py-7 sm:px-7 lg:px-10"><PageHeader eyebrow="Shared by students, for students" title="Resources." description="The notes, guides, and little pieces of context someone wishes they had earlier." action={<Button variant="soft" testId="button-upload-resource"><Plus size={17} />Share a resource</Button>} /><div className="mb-7 grid gap-4 md:grid-cols-3">{folders.map(folder => { const Icon = folder.icon; return <button key={folder.name} data-testid={`button-folder-${folder.name}`} className={`campus-card campus-card-lift flex items-center gap-4 p-4 text-left ${active === folder.name ? 'ring-2 ring-[hsl(var(--primary))]' : ''}`} onClick={() => setActive(folder.name)}><span className={`grid h-12 w-12 place-items-center rounded-xl ${folder.color}`}><Icon size={21} /></span><span><strong className="block font-serif text-lg">{folder.name}</strong><small className="text-xs text-[hsl(var(--muted-foreground))]">{folder.count}</small></span><ChevronRight className="ml-auto text-[hsl(var(--muted-foreground))]" size={17} /></button>; })}</div><div className="campus-card overflow-hidden"><div className="flex flex-col justify-between gap-3 border-b border-[hsl(var(--border))] p-5 sm:flex-row sm:items-center"><div><h2 className="font-serif text-2xl font-bold">{active}</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Study material with a little more humanity.</p></div><div className="flex gap-2"><button data-testid="button-resources-search" className="rounded-lg border border-[hsl(var(--border))] p-2.5 text-[hsl(var(--muted-foreground))]"><Search size={16} /></button><button data-testid="button-resources-filter" className="rounded-lg border border-[hsl(var(--border))] p-2.5 text-[hsl(var(--muted-foreground))]"><Filter size={16} /></button></div></div><div>{materials.map((item, i) => <div key={item.title} className="flex items-center gap-4 border-b border-[hsl(var(--border)/.7)] p-4 last:border-0 hover:bg-[hsl(var(--muted)/.35)]"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-[hsl(var(--muted))] text-[hsl(var(--primary))]"><FolderOpen size={18} /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{item.title}</p><p className="mt-1 text-[10px] text-[hsl(var(--muted-foreground))]">Shared by {item.by} · {item.time}</p></div><span className="hidden rounded-full bg-[hsl(var(--muted))] px-2.5 py-1 text-[9px] font-bold sm:inline">{item.type}</span><button aria-label={`Open ${item.title}`} data-testid={`button-open-resource-${i}`} className="rounded-lg p-2 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]"><ExternalLink size={16} /></button></div>)}</div></div></div>;
}

function OpportunitiesPage() {
  const [saved, setSaved] = useState<string[]>([]);
  const opportunities = [{ id: 'o1', type: 'Hackathon', title: 'Build for belonging', org: 'Northbridge Product Lab', deadline: 'Closes Sep 28', description: 'A weekend to make a small, useful thing for someone on campus.', color: 'bg-[#e9a68e]', icon: Lightbulb }, { id: 'o2', type: 'Research project', title: 'How students find their people', org: 'Social Computing Lab', deadline: 'Closes Oct 04', description: 'Help study the low-pressure pathways that make campus communities kinder.', color: 'bg-[#7bb4ad]', icon: BookOpen }, { id: 'o3', type: 'Internship', title: 'Product design intern', org: 'Sonder Studio', deadline: 'Closes Oct 11', description: 'A paid studio placement for someone who notices the details others miss.', color: 'bg-[#e5bd70]', icon: BriefcaseBusiness }, { id: 'o4', type: 'Startup team', title: 'Looking for a community lead', org: 'The Pocket Pantry', deadline: 'Rolling', description: 'Join a student startup making affordable food access easier on campus.', color: 'bg-[#9d91bd]', icon: UsersRound }];
  return <div className="mx-auto max-w-[1100px] px-4 py-7 sm:px-7 lg:px-10"><PageHeader eyebrow="A next step, when you want one" title="Opportunities." description="Hackathons, research, internships, and the projects that turn a passing idea into a path." action={<Button variant="soft" testId="button-post-opportunity"><Plus size={17} />Post an opportunity</Button>} /><div className="mb-7 flex gap-2 overflow-x-auto pb-1">{['All', 'Hackathons', 'Internships', 'Research', 'Startup teams'].map((x, i) => <button key={x} data-testid={`button-opportunity-filter-${x.toLowerCase().replace(' ', '-')}`} className={`whitespace-nowrap rounded-full px-4 py-2 text-xs font-bold ${i === 0 ? 'bg-[hsl(var(--sidebar))] text-white' : 'border border-[hsl(var(--border))] bg-[hsl(var(--card))] text-[hsl(var(--muted-foreground))]'}`}>{x}</button>)}</div><div className="grid gap-4 md:grid-cols-2">{opportunities.map(item => { const Icon = item.icon; return <article key={item.id} className="campus-card campus-card-lift overflow-hidden" data-testid={`card-opportunity-${item.id}`}><div className={`flex items-center gap-3 ${item.color} px-5 py-4`}><span className="grid h-9 w-9 place-items-center rounded-lg bg-white/70"><Icon size={18} /></span><span className="font-mono text-[10px] font-bold uppercase tracking-[.12em]">{item.type}</span><button aria-label="Save opportunity" data-testid={`button-save-opportunity-${item.id}`} onClick={() => setSaved(s => s.includes(item.id) ? s.filter(x => x !== item.id) : [...s, item.id])} className={`ml-auto rounded-lg p-1.5 ${saved.includes(item.id) ? 'bg-white/80' : 'hover:bg-white/40'}`}><Bookmark size={16} fill={saved.includes(item.id) ? 'currentColor' : 'none'} /></button></div><div className="p-5"><h3 className="font-serif text-2xl font-bold">{item.title}</h3><p className="mt-1 text-xs font-semibold text-[hsl(var(--muted-foreground))]">{item.org}</p><p className="mt-4 text-sm leading-6 text-[hsl(var(--muted-foreground))]">{item.description}</p><div className="mt-5 flex items-center justify-between border-t border-[hsl(var(--border))] pt-4"><span className="flex items-center gap-1.5 text-xs font-semibold text-[hsl(var(--primary))]"><Clock3 size={14} />{item.deadline}</span><Button className="px-3 py-2" testId={`button-view-opportunity-${item.id}`}>View details <ArrowRight size={14} /></Button></div></div></article>; })}</div></div>;
}

function NotificationsPage() {
  const notifications = [{ icon: UserPlus, title: 'Raghuvaran accepted your connection request.', time: '12 min ago', type: 'Connection', unread: true, color: 'bg-[#e9a68e]' }, { icon: GraduationCap, title: 'Jersey is open to a mentorship conversation.', time: '1 hr ago', type: 'Mentorship', unread: true, color: 'bg-[#7bb4ad]' }, { icon: CalendarDays, title: 'Quiet study table starts tomorrow at North Library.', time: '3 hrs ago', type: 'Event', unread: true, color: 'bg-[#e5bd70]' }, { icon: MessageCircle, title: 'Shalini sent you a message.', time: 'Yesterday', type: 'Message', unread: true, color: 'bg-[#9d91bd]' }, { icon: Building2, title: 'The Common Room shared a new resource.', time: 'Monday', type: 'Club', unread: false, color: 'bg-[#d6ccb0]' }];
  const [read, setRead] = useState<string[]>([]);
  return <div className="mx-auto max-w-[800px] px-4 py-7 sm:px-7 lg:px-10"><PageHeader eyebrow="Stay in the loop" title="Notifications." description="The small updates that help campus feel less like a crowd and more like a community." action={<Button variant="outline" onClick={() => setRead(notifications.map(n => n.title))} testId="button-mark-all-read">Mark all read</Button>} /><div className="campus-card overflow-hidden">{notifications.map((n, i) => { const Icon = n.icon; const isRead = read.includes(n.title); return <div key={n.title} className={`flex items-start gap-4 border-b border-[hsl(var(--border)/.7)] p-5 last:border-0 ${!isRead && n.unread ? 'bg-[hsl(var(--accent)/.12)]' : ''}`} data-testid={`notification-${i}`}><span className={`grid h-10 w-10 shrink-0 place-items-center rounded-xl ${n.color}`}><Icon size={18} /></span><div className="min-w-0 flex-1"><p className="text-sm leading-5">{n.title}</p><div className="mt-1 flex items-center gap-2 text-[10px] text-[hsl(var(--muted-foreground))]"><span>{n.type}</span><span>·</span><span>{n.time}</span></div></div>{!isRead && n.unread && <button onClick={() => setRead(r => [...r, n.title])} aria-label="Mark notification read" data-testid={`button-read-notification-${i}`} className="mt-1 h-2.5 w-2.5 rounded-full bg-[hsl(var(--primary))]" />}</div>; })}</div></div>;
}

function ProfilePage() {
  const [editing, setEditing] = useState(false);
  const [bio, setBio] = useState(currentStudent.bio || '');
  const [saved, setSaved] = useState(false);
  return <div className="mx-auto max-w-[1050px] px-4 py-7 sm:px-7 lg:px-10"><div className="campus-card relative mb-6 overflow-hidden"><div className="h-36 bg-[hsl(var(--sidebar))] sm:h-48"><div className="absolute -right-10 -top-28 h-72 w-72 rounded-full border-[32px] border-[hsl(var(--accent)/.16)]" /></div><div className="relative px-5 pb-6 sm:px-8"><div className="-mt-12 flex flex-col justify-between gap-4 sm:-mt-14 sm:flex-row sm:items-end"><Avatar person={currentStudent} size="xl" /><div className="flex gap-2"><Button variant="outline" onClick={() => { setEditing(!editing); setSaved(false); }} testId="button-edit-profile">{editing ? 'Cancel' : 'Edit profile'}</Button><Button variant="soft" testId="button-share-profile"><Share2 size={15} />Share</Button></div></div><div className="mt-4"><div className="flex flex-wrap items-center gap-2"><h1 className="font-serif text-3xl font-bold">{currentStudent.name}</h1><span className="flex items-center gap-1 rounded-full bg-[hsl(var(--secondary)/.13)] px-2 py-1 text-[10px] font-bold text-[hsl(var(--secondary))]"><BadgeCheck size={13} />Verified student</span></div><p className="mt-1 text-sm text-[hsl(var(--muted-foreground))]">{currentStudent.major} · {currentStudent.year} · Northbridge University</p>{editing ? <div className="mt-5"><textarea value={bio} onChange={e => setBio(e.target.value)} data-testid="input-profile-bio" className="min-h-24 w-full rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--muted)/.4)] p-3 text-sm outline-none focus:border-[hsl(var(--primary))]" /><div className="mt-3 flex items-center justify-end gap-3">{saved && <span className="text-xs font-semibold text-[hsl(var(--secondary))]">Profile saved</span>}<Button onClick={() => { setSaved(true); setEditing(false); }} testId="button-save-profile"><Check size={15} />Save changes</Button></div></div> : <p className="mt-4 max-w-xl text-sm leading-6 text-[hsl(var(--muted-foreground))]">{bio}</p>}</div><div className="mt-6 flex flex-wrap gap-2">{currentStudent.tags?.map(tag => <span key={tag} className="rounded-full bg-[hsl(var(--muted))] px-3 py-1.5 text-xs font-semibold">{tag}</span>)}</div><div className="mt-7 flex gap-8 border-t border-[hsl(var(--border))] pt-5"><div><p className="font-mono text-lg font-bold">128</p><p className="text-[10px] text-[hsl(var(--muted-foreground))]">connections</p></div><div><p className="font-mono text-lg font-bold">07</p><p className="text-[10px] text-[hsl(var(--muted-foreground))]">clubs joined</p></div><div><p className="font-mono text-lg font-bold">14</p><p className="text-[10px] text-[hsl(var(--muted-foreground))]">resources shared</p></div></div></div></div><div className="grid gap-6 md:grid-cols-[1.2fr_.8fr]"><div className="campus-card p-5 sm:p-6"><div className="mb-5 flex items-center justify-between"><h2 className="font-serif text-2xl font-bold">A little more about me</h2><button data-testid="button-profile-more" className="rounded-lg p-2 text-[hsl(var(--muted-foreground))]"><MoreHorizontal size={17} /></button></div><div className="space-y-4 text-sm"><p className="flex gap-3"><Compass size={17} className="mt-0.5 shrink-0 text-[hsl(var(--primary))]" /><span>Currently exploring <strong>civic technology</strong> and the spaces between design and public good.</span></p><p className="flex gap-3"><MapPin size={17} className="mt-0.5 shrink-0 text-[hsl(var(--primary))]" /><span>Usually found in the <strong>North Library</strong>, Studio 2, or walking the long way home.</span></p></div></div><div className="campus-card p-5 sm:p-6"><h2 className="font-serif text-2xl font-bold">My campus rooms</h2><div className="mt-5 space-y-3">{['The Common Room', 'Product Lab', 'Frame by Frame'].map((x, i) => <div key={x} className="flex items-center gap-3"><span className={`grid h-9 w-9 place-items-center rounded-lg ${['bg-[#e9a68e]', 'bg-[#e5bd70]', 'bg-[#9d91bd]'][i]}`}><Building2 size={15} /></span><span className="text-xs font-semibold">{x}</span><ChevronRight size={14} className="ml-auto text-[hsl(var(--muted-foreground))]" /></div>)}</div></div></div></div>;
}

function ComingSoon() {
  const items = [{ icon: Sparkles, title: 'AI Mentor Recommendation', copy: 'A thoughtful match, based on the kind of support you need.' }, { icon: UsersRound, title: 'Smart Teammate Finder', copy: 'Find the missing skill in your next good idea.' }, { icon: Compass, title: 'Interest Matching', copy: 'Notice the people who keep circling the same curiosities.' }, { icon: CalendarDays, title: 'Personalized Event Suggestions', copy: 'A campus calendar that learns your pace.' }];
  return <div className="campus-card overflow-hidden bg-[hsl(var(--sidebar))] text-white"><div className="p-6 sm:p-8"><div className="mb-2 flex items-center gap-2 text-[hsl(var(--sidebar-primary))]"><Sparkles size={16} /><span className="font-mono text-[10px] font-bold uppercase tracking-[.18em]">Coming soon</span></div><h2 className="font-serif text-3xl font-bold">The campus gets<br />a little more intuitive.</h2><p className="mt-3 max-w-md text-sm leading-6 text-white/55">We are building the next layer of CampusOne with care. Tell us what would be useful first.</p></div><div className="grid border-t border-white/10 sm:grid-cols-2">{items.map((item, i) => { const Icon = item.icon; return <div key={item.title} className={`border-b border-white/10 p-5 ${i % 2 === 1 ? 'sm:border-l' : ''} ${i >= 2 ? 'sm:border-b-0' : ''}`}><Icon size={18} className="mb-5 text-[hsl(var(--sidebar-primary))]" /><h3 className="text-sm font-bold">{item.title}</h3><p className="mt-2 text-xs leading-5 text-white/45">{item.copy}</p></div>; })}</div></div>;
}

function SettingsPage() {
  const [prefs, setPrefs] = useState({ email: true, messages: true, events: false, digest: true, private: false });
  const toggle = (key: keyof typeof prefs) => setPrefs(p => ({ ...p, [key]: !p[key] }));
  const rows: [keyof typeof prefs, string, string][] = [['email', 'Connection activity', 'When someone connects, follows, or responds to you.'], ['messages', 'Messages', 'When someone sends you a new message.'], ['events', 'Event reminders', 'A gentle nudge before events you saved.'], ['digest', 'Weekly campus digest', 'A considered handful of things you may have missed.'], ['private', 'Quiet mode', 'Hide your profile from people outside your connections.']];
  return <div className="mx-auto max-w-[820px] px-4 py-7 sm:px-7 lg:px-10"><PageHeader eyebrow="Make this space yours" title="Settings." description="Your account, your boundaries, your pace." /><div className="space-y-5"><section className="campus-card overflow-hidden"><div className="border-b border-[hsl(var(--border))] p-5 sm:p-6"><h2 className="font-serif text-2xl font-bold">Notifications & privacy</h2><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">Choose what deserves your attention.</p></div>{rows.map(([key, title, copy]) => <div key={key} className="flex items-center gap-4 border-b border-[hsl(var(--border)/.7)] p-5 last:border-0"><div className="flex-1"><p className="text-sm font-bold">{title}</p><p className="mt-1 text-xs leading-5 text-[hsl(var(--muted-foreground))]">{copy}</p></div><button role="switch" aria-checked={prefs[key]} onClick={() => toggle(key)} data-testid={`switch-${key}`} className={`relative h-7 w-12 shrink-0 rounded-full p-1 transition-colors ${prefs[key] ? 'bg-[hsl(var(--secondary))]' : 'bg-[hsl(var(--muted))]'}`}><span className={`block h-5 w-5 rounded-full bg-white shadow-sm transition-transform ${prefs[key] ? 'translate-x-5' : ''}`} /></button></div>)}</section><section className="campus-card p-5 sm:p-6"><h2 className="font-serif text-2xl font-bold">Account</h2><div className="mt-5 space-y-3"><div className="flex items-center gap-3 rounded-xl bg-[hsl(var(--muted)/.5)] p-3"><Mail size={17} className="text-[hsl(var(--primary))]" /><div className="flex-1"><p className="text-xs font-bold">College email</p><p className="text-xs text-[hsl(var(--muted-foreground))]">maya.rao@northbridge.edu</p></div><span className="flex items-center gap-1 text-[10px] font-bold text-[hsl(var(--secondary))]"><Check size={13} />Verified</span></div><button data-testid="button-change-password" className="flex w-full items-center gap-3 rounded-xl border border-[hsl(var(--border))] p-3 text-left text-sm font-semibold hover:bg-[hsl(var(--muted))]"><LockKeyhole size={17} className="text-[hsl(var(--primary))]" />Change password <ChevronRight size={16} className="ml-auto" /></button><button data-testid="button-help-settings" className="flex w-full items-center gap-3 rounded-xl border border-[hsl(var(--border))] p-3 text-left text-sm font-semibold hover:bg-[hsl(var(--muted))]"><CircleHelp size={17} className="text-[hsl(var(--primary))]" />Help & support <ChevronRight size={16} className="ml-auto" /></button></div></section><ComingSoon /><button data-testid="button-delete-account" className="w-full py-3 text-xs font-semibold text-[hsl(var(--destructive))]">Deactivate my CampusOne account</button></div></div>;
}

function EmptyState({ icon: Icon, title, copy, action }: { icon: typeof Search; title: string; copy: string; action?: ReactNode }) {
  return <div className="campus-card flex min-h-[280px] flex-col items-center justify-center p-8 text-center"><span className="mb-4 grid h-14 w-14 place-items-center rounded-2xl bg-[hsl(var(--accent))]"><Icon size={24} /></span><h3 className="font-serif text-2xl font-bold">{title}</h3><p className="mt-2 max-w-sm text-sm leading-6 text-[hsl(var(--muted-foreground))]">{copy}</p>{action && <div className="mt-5">{action}</div>}</div>;
}

function Router() {
  const [location] = useLocation();
  if (location === '/login' || location === '/signup') return <Switch><Route path="/login"><AuthPage mode="login" /></Route><Route path="/signup"><AuthPage mode="signup" /></Route></Switch>;
  return <AppShell><Switch><Route path="/" component={HomePage} /><Route path="/dashboard" component={HomePage} /><Route path="/profile" component={ProfilePage} /><Route path="/connections" component={ConnectionsPage} /><Route path="/messages" component={MessagesPage} /><Route path="/mentors" component={MentorsPage} /><Route path="/clubs" component={ClubsPage} /><Route path="/events" component={EventsPage} /><Route path="/resources" component={ResourcesPage} /><Route path="/opportunities" component={OpportunitiesPage} /><Route path="/notifications" component={NotificationsPage} /><Route path="/settings" component={SettingsPage} /><Route><EmptyState icon={Compass} title="Page not found" copy="This campus path does not exist yet." action={<Link href="/" data-testid="link-not-found-home" className="inline-flex rounded-xl bg-[hsl(var(--primary))] px-4 py-2.5 text-sm font-bold text-white">Back home</Link>} /></Route></Switch></AppShell>;
}

function App() {
  return <QueryClientProvider client={queryClient}><TooltipProvider><ErrorBoundary><Router /></ErrorBoundary><Toaster /></TooltipProvider></QueryClientProvider>;
}

export default App;
